var app = angular.module('app', ["ngResource", "ngCookies"]);


app.config(["$routeProvider", function($routeProvider) {
  $routeProvider
  .when('/', {templateUrl: '<%= asset_path "conenctions.html" %>', controller: 'ConnectionsCtrl'})

}])
